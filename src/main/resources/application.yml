spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ecommerce
    username: root
    password: YOUR_PASSWORD

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080